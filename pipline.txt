1. Пользователь → Telegram-бот → LLM + RAG → Ответ
2. RAG Pipeline:
   - Загрузка данных (PDF, статьи) → Разбивка на чанки → Эмбеддинги → Векторная БД
   - Поиск по запросу → Передача релевантных чанков в LLM → Генерация ответа
3. Генерация рациона:
   - Запрос параметров (вес, рост, активность, цель) → Расчёт калорий → Генерация меню LLM